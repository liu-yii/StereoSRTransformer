test_dataset:
  dataset:
    name: image-folder
    args:
      root_path: /media/yi/E/Research/Datasets/AID_SR/AID_valid_64_512/hr_512
  wrapper:
    name: sr-implicit-downsampled
    args:
      scale_min: 30
  batch_size: 1
eval_type: div2k-30
eval_bsize: 30000
save_dir: "results/AID-x30"

data_norm:
  inp: {sub: [0.5], div: [0.5]}
  gt: {sub: [0.5], div: [0.5]}